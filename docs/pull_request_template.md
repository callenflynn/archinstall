## PR Description:

<!-- Please describe what changes this PR introduces, a good example would be: https://github.com/archlinux/archinstall/pull/1377 -->

- This fix/feature relates to: <!-- issue #13, feature request, ... -->

## Tests and Checks

- [ ] Unit tests pass locally: `pytest tests/`
- [ ] Lint and format are clean: `ruff check archinstall/ tests/ && ruff format --check archinstall/ tests/`
- [ ] Type check is clean: `mypy` (project config, includes `stubs/`)
- [ ] Docs build without errors: `sphinx-build -b html docs _build` (when `docs/` changed)
- [ ] I have tested the code!<br>
  <!--
      After submitting your PR, an ISO can be downloaded below the PR description. After testing it you can check the box
      You can do manual tests too, like isolated function tests, just something!
  -->
